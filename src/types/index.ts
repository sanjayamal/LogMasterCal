export type ThemeVariant = "blue" | "purple" | "teal" | "amber" | "default";

export type IconVariant = "blue" | "purple" | "teal";

export interface StepItem {
  label?: string;
  expression: string;
  note?: string;
}
